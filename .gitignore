# ── Secrets & credentials ─────────────────────────────────────────────────────
**/.env
**/.env.local
**/.env.*.local
**/secrets/
**/*service-account*.json
**/*firebase*.json
!firebase.json
!firestore.indexes.json
**/*credentials*.json
**/*keyfile*.json
**/*private-key*.json

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.pytest_cache/
*.egg-info/
.mypy_cache/

# ── Security audit reports (contain CVE details — keep out of VCS) ───────────
**/pip-audit-report.json
**/pip-audit-sbom.json
**/npm-audit-report.json

# ── ChromaDB / RAG data (large binary — not for VCS) ─────────────────────────
**/data/chroma_db/
**/chroma.sqlite3

# ── Node / Expo ───────────────────────────────────────────────────────────────
node_modules/
.expo/
dist/
build/
*.log

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
